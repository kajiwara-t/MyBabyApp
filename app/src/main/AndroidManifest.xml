<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sunrisesystem.mybabyapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Opening_Activity"
            android:label="MyBabyApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".List_Activity" />
        <activity android:name=".BMI_Activity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".BMI_Output_Activity" />
        <activity android:name=".Record_List_Activity" />
        <activity android:name=".Chart_Activity" />
        <activity android:name=".BMI_Select" />
        <activity android:name=".Sub_Record_List_Activity" />
        <activity android:name=".Female_Chart_Activity" />
        <activity android:name=".Male_BMI_Chart" />
        <activity android:name=".Female_BMI_Chart"></activity>
    </application>

</manifest>